---
import Icon from "astro-icon";

export interface Props {
  label: string;
  icon: string;
}

const { label, icon } = Astro.props;
---

<div
  class="flex items-center gap-2 bg-[#FFEAE4] text-[#93153B] px-6 py-3 text-sm"
>
  <Icon pack="mdi" name={icon} size={20} />
  <span>{label}</span>
</div>
