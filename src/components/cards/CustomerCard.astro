---
import Icon from "astro-icon";

export interface Props {
  label: string;
  icon: string;
}

const { label, icon } = Astro.props;
---

<div
  class="flex items-center gap-2 bg-[#FFF0D8] text-[#FF9456] px-6 py-3 text-sm"
>
  <Icon pack="mdi" name={icon} size={20} />
  <span>{label}</span>
</div>
